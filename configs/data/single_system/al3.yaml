_target_: src.data.single_peptide_datamodule.SinglePeptideDataModule
data_dir: ${paths.data_dir}sbg_data
sequence: AAA
temperature: 310
batch_size: 512 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False
num_dimensions: 3
num_atoms: 33       # Max number of particles + at least a single padding token
dim: 99