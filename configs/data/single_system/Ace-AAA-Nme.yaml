_target_: src.data.single_peptide_datamodule.SinglePeptideDataModule
repo_id: transferable-samplers/sequential-boltzmann-generators-data
data_dir: ${paths.scratch_dir}/sequential-boltzmann-generators-data
sequence: Ace-AAA-Nme
temperature: 300
batch_size: 512 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False
num_dimensions: 3
num_atoms: 42 # Max number of particles + at least a single padding token
