# @package _global_

defaults:
  - override /model: normalizing_flow
  - override /model/net: tarflow_single_system
  - override /data: single_system/Ace-A-Nme
  - override /trainer: gpu

model:
  net:
    in_channels: 1
    img_size: 66
    num_blocks: 4
    layers_per_block: 4

callbacks:
  sampling_evaluation:
    sampler:
      _target_: src.models.samplers.smc_sampler.SMCSampler
      num_samples: 10_000
      proposal_batch_size: 10_000
      kernel_type: ula
      langevin_eps: 1e-7
      num_annealing_steps: 100
      ess_threshold: 0.5
      systematic_resampling: false
      warmup: 0.0
      gradient_batch_size: 128
      input_energy_filter_cutoff: 10.0
      use_com_adjustment: true
      logit_clip_filter: 0.002
      do_energy_plots: true
      log_freq: 1

data:
  batch_size: 10_000
  num_eval_samples: 10_000

trainer:
  limit_test_batches: 1
  limit_val_batches: 1

hf_state_dict_path: single_system/tarflow_Ace-A-Nme_0.pth
