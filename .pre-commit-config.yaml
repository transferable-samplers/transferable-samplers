default_language_version:
  python: python3

repos:
  # --------------------------------------------
  # Python: Linting, formatting, import sorting,
  # pyupgrade, security (Bandit), debug detection
  # --------------------------------------------
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.2
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # --------------------------------------------
  # Essential pre-commit housekeeping hooks
  # (not covered by Ruff)
  # --------------------------------------------
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        exclude: .svg$
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-case-conflict
      - id: check-added-large-files
      - id: check-executables-have-shebangs

  # --------------------------------------------
  # Secret scanning
  # --------------------------------------------
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks

  # --------------------------------------------
  # YAML formatting
  # --------------------------------------------
  - repo: https://github.com/google/yamlfmt
    rev: v0.21.0
    hooks:
      - id: yamlfmt

  # --------------------------------------------
  # Shell linting
  # --------------------------------------------
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.9.0.6
    hooks:
      - id: shellcheck
        args: ["-e", "SC2102"]

  # --------------------------------------------
  # Spelling checker
  # --------------------------------------------
  - repo: https://github.com/codespell-project/codespell
    rev: v2.2.6
    hooks:
      - id: codespell
        args:
          - --ignore-words-list=ot,nd,ser,manuel

  # --------------------------------------------
  # Type checking (manual stage — run explicitly
  # with: pre-commit run pyrefly --all-files)
  # --------------------------------------------
  - repo: https://github.com/facebook/pyrefly-pre-commit
    rev: "0.54.0"
    hooks:
      - id: pyrefly-check
        stages: [manual]
        pass_filenames: false
